<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <eightbitlab.com.blurview.BlurView
        android:id="@+id/glass_blur_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@id/tv_tooltip_text"
        app:layout_constraintBottom_toBottomOf="@id/tv_tooltip_text"
        app:layout_constraintStart_toStartOf="@id/tv_tooltip_text"
        app:layout_constraintEnd_toEndOf="@id/tv_tooltip_text"
        android:background="@drawable/bg_glass_tooltip"
        app:blurOverlayColor="@android:color/transparent" />

    <TextView
        android:id="@+id/tv_tooltip_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:maxWidth="280dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:lineSpacingExtra="3dp"
        android:fontFamily="@font/geologica_regular"
        app:fontFamily="@font/geologica_regular" />

</androidx.constraintlayout.widget.ConstraintLayout>
